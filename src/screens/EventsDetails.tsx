import React, { useEffect } from 'react';
import { VStack, Image, Text } from "native-base";



export function EventsDetails({ route }) {
  // useEffect(() => {
  //   // Use eventData conforme necessário
  //   console.log('Event Data in EventDetails:', eventData);
  // }, [eventData]);

  return (

    <Text color='gray.100' fontSize='sm' fontWeight={400} mt={20}> Event Details</Text>
  );
}

